import { Text, TouchableOpacity, View } from "react-native";
import { Button } from "../../../../ui/button";

export function SingUpLink() {
  return (
    <View
        style={{
            alignItems: 'center',
            justifyContent: 'center',
            marginBottom: '20%'
        }}
    >
      <Button onPress={() => {}} text="Realizar Cadastro" />
    </View>
  );
}
 