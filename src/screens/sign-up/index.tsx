import { SafeAreaView } from "react-native";
import { Logo } from "../../components/logo";

export function SignUp() {
  return (
    <SafeAreaView>
      <Logo fontSize={36}/>
    </SafeAreaView>
  );
}
